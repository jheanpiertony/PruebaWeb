@model CommonCore.Compra
@using CommonCore

@{
    ViewData["Title"] = "Create";
    var a = Model;
}

<h1>Create</h1>

<h4>Compra</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ApplicationUser" class="control-label"></label>
                <select asp-for="ApplicationUser" asp-items="ViewBag.ApplicationUser" class="form-control"></select>
            </div>
            <div class="form-group">
                <label asp-for="FechaCompra" class="control-label"></label>
                <input asp-for="FechaCompra" class="form-control" disabled="disabled" />
                @*<span asp-validation-for="FechaCompra" class="text-danger"></span>*@
            </div>
            @*<div class="form-group form-check">
                    <label class="form-check-label">
                        <input class="form-check-input" asp-for="EstaBorrado" /> @Html.DisplayNameFor(model => model.EstaBorrado)
                    </label>
                </div>*@
        </form>
        <div class="form-group">
            <input type="submit" value="Create" class="btn btn-primary" />
        <from method="post" data-ajax="true" data-ajax-method="post">
            <input type="submit" value="Agregar producto" class="btn btn-primary" />
        </from>
        </div>
        <div class="form-group">
            <label class="control-label">Producto</label>
            <select id="Productos" name="Productos" asp-items="@ViewBag.Productos" class="form-control"></select>
        </div>
    </div>
</div>
<div id="ListaPoductos">
    @await Html.PartialAsync("_ListaProductos", TempData["ListaProductos"])
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(function () {
        });
    </script>
}
