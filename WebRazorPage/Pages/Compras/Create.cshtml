@page
@model WebRazorPage.Pages.Compras.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Compra</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Compra.ApplicationUser" class="control-label"></label>
                <select asp-for="Compra.ApplicationUser" asp-items="@((SelectList)ViewData["ListaApplicationUser"])" class="form-control"></select>
            </div>
            <input type="submit" value="Create" class="btn btn-primary" />
        </form>
        @*<div class="form-group">            
            <from method="post">
                <label class="control-label">Producto</label>
                <select asp-for="ProductoSeleccionado" asp-items="@((SelectList)ViewData["ListaProductos"])" class="form-control"></select>
                <input type="submit" value="Agregar producto" class="btn btn-primary" asp-page-handler="ListaProductosPartical" />
            </from>
        </div>*@
    </div>
</div>
@*<div id="ListaPoductos">
    @await Html.PartialAsync("_ListaProductos", Model.ListaProductosPartical)
</div>*@

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
